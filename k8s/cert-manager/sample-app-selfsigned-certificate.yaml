apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sample-app-selfsigned
  namespace: sample-app
spec:
  secretName: sample-app-selfsigned-tls
  commonName: sample-app.local
  dnsNames:
    - sample-app.local
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
